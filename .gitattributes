# Files that run on the Pi must keep LF endings — CRLF breaks shell scripts
# ("\r: command not found") and systemd units, and this repo is edited on Windows.
*.sh      text eol=lf
*.service text eol=lf
*.timer   text eol=lf
*.yml     text eol=lf
